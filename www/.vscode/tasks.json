{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start dev server",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "up", "--build"],
      "options": {
        "env": {
          "MODE": "development"
        }
      },
      "problemMatcher": []
    },
    {
      "label": "start prod server",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "up", "--build"],
      "options": {
        "env": {
          "MODE": "production"
        }
      },
      "problemMatcher": []
    },
    {
      "label": "start container and halt",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "up", "--build"],
      "options": {
        "env": {
          "MODE": "sleep"
        }
      },
      "problemMatcher": []
    }
  ]
}
