---
title: spacer
description: Inserts vertical whitespace between elements.
---

`::spacer` renders an invisible `div` of a fixed height. Use it to add breathing
room between sections when the default paragraph spacing is not enough.

Maps to the `<Spacer />` React component via the kebab-case → PascalCase
directive convention.

## Reference

```mdx
::spacer ::spacer{size=xl}
```

| Attribute | Type                                            | Required | Default | Description               |
| --------- | ----------------------------------------------- | -------- | ------- | ------------------------- |
| `size`    | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| '2xl'` | No       | `'md'`  | Amount of vertical space. |

| `size` | Height |
| ------ | ------ |
| `xs`   | 8px    |
| `sm`   | 16px   |
| `md`   | 32px   |
| `lg`   | 48px   |
| `xl`   | 64px   |
| `2xl`  | 96px   |

## Examples

Each example below uses a horizontal rule immediately after the spacer to make
the gap visible.

### xs — 8px

```mdx
::spacer{size=xs}
```

::spacer{size=xs}

---

### sm — 16px

```mdx
::spacer{size=sm}
```

::spacer{size=sm}

---

### md — 32px (default)

```mdx
::spacer
```

::spacer

---

### lg — 48px

```mdx
::spacer{size=lg}
```

::spacer{size=lg}

---

### xl — 64px

```mdx
::spacer{size=xl}
```

::spacer{size=xl}

---

### 2xl — 96px

```mdx
::spacer{size=2xl}
```

::spacer{size=2xl}

---
